---
const posts = [
  { title: "My First Astro Essay", url: "/first-essay" }
];
---

<h1>Tom’s Lab</h1>
<p>A Markdown-native writing experiment</p>

<ul>
  {posts.map(post => (
    <li><a href={post.url}>{post.title}</a></li>
  ))}
</ul>